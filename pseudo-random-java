/* Data Structures and Algorithms in Java, 6th Edition…dent Version
   Chapter 3
   Give the next ﬁve pseudorandomnumbers generated by the process described on page 113, with a = 12, b = 5, 
   and n = 100, and 92 as the seed for cur.
*/


// generate a pseudo-random number using the formula next = (a ∗ cur + b) [n] 
public int generateRandom(int a, int b, int n, int cur){
	return (a*cur + b) %n;
}


// generate n pseudo-random numbers
public int[] generateNRandom(int a, int b, int m, int cur, int n){
	int[] result = new int[n];
	for(int i=0; i<n; i++){
		cur = generateRandom(a,b,m,cur);
		System.out.println(cur);
		result[i]=cur;
	}
	return result;
}

public void testRand(){
	int[] randNumbers = generateNRandom(12,5,100,92,5);
	for (int j = 0; j<randNumbers.length; j++){
		System.out.printf("The %d number is %d\n",j+1,randNumbers[j]);
	}
}

tesRand();
